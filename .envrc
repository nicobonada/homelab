# Workstation tools: docker client/compose + sops + TUIs (see flake.nix).
# The flake sets DOCKER_HOST=ssh://nico@homelab (override in the environment if needed).
# Requires direnv + nix-direnv, then `direnv allow`.
use flake
