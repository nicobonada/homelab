/result
/result-*
.direnv/

# accidental local secrets
token

# Decrypted stack env (from sops -d secrets/<stack>.env); never commit plaintext
stacks/**/.env
stacks/**/secrets.env
stacks/**/*-admin.env
